// TagChips is no longer used in v3. The daily tracking system replaces tags.
// Kept as a stub to avoid dead import errors.

export function TagChips(_props: { selected: string[]; onToggle: (id: string) => void }) {
  return null;
}
